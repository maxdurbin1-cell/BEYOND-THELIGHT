# QoL Implementation Backlog

## Status
- Implemented in this pass:
- Global selected-state readability pass across Province, Last Sea, and World That Was maps (selected glow + dim non-selected nodes).
- Compact context quick-action strip under Quick Access for high-frequency actions by active tab.

## Now (P0)
- Normalize selected-state visuals for remaining tactical surfaces (planet exploration, exocraft routes, special combat boards).
- Add tiny save/sync heartbeat status in header (Last Save, Local/Remote Sync, Dirty State).
- Add one-click recovery actions for reconnect/desync in campaign mode.

## Next (P1)
- Expand context quick-action strip coverage for additional tabs with role-aware actions (Player vs GM).
- Add keyboard hotkeys for common actions (open missions, next round, map click mode toggle).
- Introduce contextual toast summaries after major actions (travel, mission state changes, combat transitions).

## Later (P2)
- Add global command palette style launcher for tabs and systems.
- Add optional accessibility presets for high-contrast tactical overlays.
- Add lightweight workflow analytics (which actions are used most) to refine quick actions.
